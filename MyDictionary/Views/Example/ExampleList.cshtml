@model List<DictionaryDto.ExampleDto>

@{
    if (Model.Count() > 0)
    {
        <ul style="list-style-type:disc;">
            @for (int index = 0; index < Model.Count(); index++)
            {
                <li style="padding-top: 10px; padding-bottom: 10px;">
                    @Model.ElementAt(index).ExampleText
                </li>
            }
        </ul>
    }
}
